# Rust Key-Value Database (KVDB)

Um banco de dados chave-valor em Rust com interface TUI (Terminal UI), B-Tree para indexação e persistência em disco.

## ✨ Funcionalidades

- ✅ **CRUD Completo**: Create, Read, Update, Delete
- 🌳 **B-Tree Index**: Indexação eficiente com balanceamento automático
- 💾 **Persistência**: Armazenamento em disco com sistema de paginação
- 🖥️ **TUI Interface**: Interface terminal intuitiva com ratatui
- 🔄 **Transações**: Operações atômicas com rollback

## 🚀 Como Usar

```bash
# Clone o repositório
git clone https://github.com/seu-usuario/Rust-KVDB.git

# Entre no diretório
cd Rust-KVDB

# Execute
cargo run --release
